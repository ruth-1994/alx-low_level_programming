#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)
{
int n;
srand(time(0));
n=rand() - RAND_MAX / 2;
return(0);
}
if (n>0)
{
printf("%i,n is positive\n")
}
else if(n == 0)
{
printf ("%i, n is zero\n");
}
else if (n<0)
{
printf("%i,n is negative\n");
}
